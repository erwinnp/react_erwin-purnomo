import ListClients from './ListGallery';

const Gallery = () => {
  return (
    <section className='wrapper d-flex align-items-center justify-content-between'>
      <ListClients />
    </section>
  );
};

export default Gallery;
